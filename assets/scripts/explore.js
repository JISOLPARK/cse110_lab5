// explore.js

window.addEventListener('DOMContentLoaded', init);
const voiceSelect = documnet.getElementById('voice-select');
let voices = []
const speech = window.speechSynthesis;
voices = speech.getVoices();


function init() {

}